# Streamlit Apps

Collection of Streamlit projects.

## How to run

1. Create virtual env:
   python -m venv venv
2. Activate:
   & "venv/Scripts/Activate.ps1"
3. Install requirements:
   pip install -r requirements.txt
4. Run app:
   streamlit run app/main.py
